import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    fontSize: 40,
  },
  input: {
    fontSize: 40,
    width: 400,
    padding: 10,
    borderColor: "#000",
    borderWidth: 2,
  },
  inputSm: {
    fontSize: 32,
    width: "100%",
    padding: 10,
    borderColor: "#111",
    borderWidth: 1,
  },
});
